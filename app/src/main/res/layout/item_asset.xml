<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_asset_row"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="8dp"
    android:paddingVertical="8dp"
    android:layout_marginBottom="4dp">

    <ImageView
        android:id="@+id/iv_asset_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:src="@drawable/ic_coin_default"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/tv_asset_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_weight="1.2"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="بیت‌کوین"
        android:textColor="@color/text_primary"
        android:textSize="13sp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="end"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_asset_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="۰"
            android:textColor="@color/text_primary"
            android:textSize="12sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/toman"
            android:textColor="@color/text_secondary"
            android:textSize="9sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_asset_change"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="0.75"
        android:background="@drawable/bg_change_badge"
        android:gravity="center"
        android:paddingHorizontal="6dp"
        android:paddingVertical="3dp"
        android:text="-0.00%"
        android:textColor="@color/negative"
        android:textSize="11sp"
        android:textStyle="bold" />
</LinearLayout>
